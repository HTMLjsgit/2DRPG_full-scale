using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemImageGetScript : MonoBehaviour
{
    public static ItemImageGetScript item_get;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Awake()
    {
        item_get = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
